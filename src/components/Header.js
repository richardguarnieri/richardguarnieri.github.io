import Navbar from './Navbar';

const Header = () => {
  return (
    <div>
        {/* Header */}
        <header className="bg-gradient-to-r from-slate-900 to-slate-700 border-b border-slate-800">
            <Navbar />
        </header>
    </div>
  )
}

export default Header